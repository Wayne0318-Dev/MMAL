# 复制为 .env.local 后填写。不要把口令和 Token 提交到 Git。

# 免费云库（Turso）。配好后导入写入云端，关电脑也能查。
# 中国大陆若打不开 turso.tech，改用香港/国内轻量 + Docker，见 DEPLOY.md 方案 B。
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# 导入/删除表格的口令。部署到 Vercel 或服务器后必须设置。
IMPORT_KEY=

# 方案 B（Docker / 轻量服务器）才需要。不要在本机开发时打开。
# HOSTING=vps

